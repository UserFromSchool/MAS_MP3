package enums;

/**
 * Discriminator for the lease contract counter-party aspect. A contract is signed
 * either with another business entity (B2B) or with a private consumer (B2C).
 */
public enum ContractType {
    B2B,
    B2C
}
